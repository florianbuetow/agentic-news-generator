# Environment variables for Agentic News Generator
# Copy this file to .env and configure with your actual values

# LM Studio API Key
# LM Studio runs locally and doesn't require real authentication
# but the application expects this variable to be set
LMSTUDIO_API_KEY=not-needed

# Optional: Anthropic API Key (if using Claude via API instead of local LLM)
# ANTHROPIC_API_KEY=your-api-key-here

# Optional: OpenAI API Key (if using OpenAI models)
# OPENAI_API_KEY=your-api-key-here
