"""Isolated transcript/summary analytics: corpus indexing, themes, timeline, digest.

This package reads only the cleaned-and-summarized corpus plus metadata, uses
no LLM, and is importable independently of the video/URL pipelines.
"""
